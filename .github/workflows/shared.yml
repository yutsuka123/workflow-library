name: 共通処理ワークフロー

on:
  workflow_call:
    inputs:
      message:
        required: true
        type: string

jobs:
  print-message:
    runs-on: ubuntu-latest
    steps:
      - name: メッセージ出力
        run: |
          echo "共通ワークフロー: ${{ inputs.message }}"
